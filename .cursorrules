# Pal Universe — Cursor 启动指令

你每次加载此项目时，按以下顺序读取上下文：

1. `CONTRIBUTING.md` — 项目协作规范、角色分工
2. `PROJECT_CONTEXT.md` — 项目全景、技术栈、进度
3. `docs/architecture/ARCHITECTURE.html` — 系统架构总览
4. `.specify/spec.md` — 功能规格
5. `.specify/plan.md` — 技术方案
6. `.specify/constitution.md` — 项目宪章（编码规范、质量门禁）

然后检查 `.specify/tasks/` 中是否有 `assigned_to: cursor` + `status: pending` 的任务。

## 可用 MCP

- codebase-memory-mcp: 用于快速查找已有代码模式和组件
- code-review-graph: 用于理解代码结构和变更影响

## 关键路径

```
frontend/src/
  components/   ← React 组件
  lib/          ← 工具函数
  data/         ← Mock JSON 数据
  app/          ← Next.js App Router 页面

scripts/        ← Python 数据脚本
.specify/       ← 任务和契约
docs/           ← 文档
```
